import { SectionTitle } from "@/components/section-title"

export const LeaguesTable = () => {
    return (
        <div className="lg:col-span-1 lg:row-span-1 max-h-[480px] bg-red-300/30">
            <SectionTitle>
                Leagues
            </SectionTitle>
        </div>
    )
}